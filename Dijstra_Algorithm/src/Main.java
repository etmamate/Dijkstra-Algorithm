import javax.swing.JFrame;

public class Main {
    public static void main(String[] args) {
        JFrame windowFrame = new JFrame();
        Window window = new Window();
        window.WindowConfiguration(windowFrame);
    }
}
